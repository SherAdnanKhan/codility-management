$(function() {
    var t = new Date;
    var time = {!! \Carbon\Carbon::now()->timestamp!!}
    alert(time);
    if (Carbon:: now()) {
        alert(t.getHours()  + ":" + t.getMinutes());
        var e = new Date(t.getFullYear(), t.getMonth(), t.getDate(), -24, 0, 0, 0);
    }
    else
    {e = new Date(t.getFullYear(), t.getMonth(), t.getDate(), 0, 0, 0, 0);
    }
    alert(e);
    $("#date").datetimepicker({
        format: "l",
        minDate: e,
        maxDate: new Date
    }), $("#time_taken").datetimepicker({
        format: "H:mm"
    })
}), $("#button_clear").click(function() {
    $('#task input[type="text"]').val(""), $("#task textarea").val("")
});